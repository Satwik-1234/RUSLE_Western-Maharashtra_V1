# ─────────────────────────────────────────────────────────────────────────────
# Western Maharashtra Soil Erosion Analysis — Python Dependencies
# Install: pip install -r requirements.txt
# Google Colab: !pip install -q -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Google Earth Engine & Geospatial ─────────────────────────────────────────
earthengine-api>=0.1.370
geemap>=0.30.0
folium>=0.14.0

# ── Scientific Computing ──────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.3.0
statsmodels>=0.14.0

# ── Visualisation ────────────────────────────────────────────────────────────
plotly>=5.17.0
kaleido>=0.2.1          # For static PNG/PDF export from Plotly
streamlit>=1.28.0       # Interactive dashboard

# ── Optional: Enhanced ML ────────────────────────────────────────────────────
shap>=0.43.0            # SHAP feature importance (falls back to permutation)
umap-learn>=0.5.4       # UMAP embedding (falls back to t-SNE only)

# ── Jupyter / Notebook ───────────────────────────────────────────────────────
ipython>=8.0.0
ipywidgets>=8.0.0
jupyter>=1.0.0
jupyterlab>=4.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.65.0
requests>=2.31.0
